// Convenience re-export so screens can `import { useAuth } from '@/hooks/useAuth'`.
// The provider + implementation live in the auth store.
export { useAuth, AuthProvider } from '@/store/auth.store';
