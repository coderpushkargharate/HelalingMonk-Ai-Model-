// App-wide constant values (client-safe).

/** localStorage key holding the JWT session token. */
export const TOKEN_STORAGE_KEY = 'hm_token';

/** localStorage key prefix for cached guest assessment reports. */
export const REPORT_STORAGE_PREFIX = 'hm_report_';
